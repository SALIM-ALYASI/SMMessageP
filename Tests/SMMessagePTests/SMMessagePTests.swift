    import XCTest
    @testable import SMMessageP

    final class SMMessagePTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(SMMessageP().text, "Hello, World!")
        }
    }
